\documentclass{article}
\usepackage[hidelinks]{hyperref}

\begin{filecontents}[overwrite]{test_frontmatter_article.fl_template.tex}
  \title{\frontloader@title}

  \author{
    \ForEachAuthor[separator={\\}]{
      \AuthorFirstMLastName{}% <- No space
      \IfAuthorHasEmail{%
        \ % <- space (possibly breaking)
        (\AuthorEmail{})
      }%
    }
  }
  
  \newcommand{\FrontloaderFrontmatter}{%
    % Generate title page
    \maketitle
  }
\end{filecontents}

% ╭───────────────────────────────────────────────╮
% │ ╭───────────────────────────────────────────╮ │
% │ │             Front Matter File             │ │
% │ ╰───────────────────────────────────────────╯ │
% ╰───────────────────────────────────────────────╯
\begin{filecontents}[overwrite]{test_frontloader_empty.fl_values.tex}
  \begin{frontloaderValues}
      % Empty...
  \end{frontloaderValues}
\end{filecontents}

\usepackage[
  values/file      = test_frontloader_empty.fl_values.tex,
  template/file    = test_frontmatter_article.fl_template.tex,
  debug
  % templates/path = frontloader_templates
]{frontloader}

\begin{document}

  \FrontloaderFrontmatter{}
  \FrontloaderDebugInfo{}

  % \ExplSyntaxOn % Enable LaTeX3 Programming layer
  % Results from LS (might be useful for getting a list of available templates):
  %   \sys_get_query:nN {ls} \ls_results
  %   \ \ls_results
  % Here is more content.
  % \ExplSyntaxOff% Disable LaTeX3 Programming layer

  \section{Introduction}
  Test a citation~\cite{goebelHybridDynamicalSystems2012}.

  % Here is my document.
  \FrontloaderBibliography{}
\end{document}

